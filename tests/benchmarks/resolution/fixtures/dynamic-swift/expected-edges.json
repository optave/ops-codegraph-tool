{
  "$schema": "../../expected-edges.schema.json",
  "language": "swift",
  "description": "Phase 6 Swift fixture: NSSelectorFromString resolved as reflection (top-level fns unqualified).",
  "edges": [
    {
      "source": { "name": "runNSSelectorFromString", "file": "dispatch.swift" },
      "target": { "name": "greet", "file": "dispatch.swift" },
      "kind": "calls",
      "mode": "dynamic",
      "notes": "NSSelectorFromString('greet') — reflection kind, resolves to top-level greet()"
    },
    {
      "source": { "name": "runNSSelectorFarewell", "file": "dispatch.swift" },
      "target": { "name": "farewell", "file": "dispatch.swift" },
      "kind": "calls",
      "mode": "dynamic",
      "notes": "NSSelectorFromString('farewell') — reflection kind, resolves to top-level farewell()"
    }
  ]
}
