{
  "$schema": "../../expected-edges.schema.json",
  "language": "c",
  "description": "Phase 5 C fixture: (*fp)() flagged unresolved-dynamic; dlsym('greet') resolved as reflection.",
  "edges": [
    {
      "source": { "name": "runDlsym", "file": "dispatch.c" },
      "target": { "name": "greet", "file": "dispatch.c" },
      "kind": "calls",
      "mode": "dynamic",
      "notes": "dlsym(handle, 'greet') — reflection kind, resolves to greet() in same unit"
    }
  ]
}
