{
  "$schema": "../../expected-edges.schema.json",
  "language": "lua",
  "description": "Phase 6 Lua fixture: t['greet']() resolved; load(code) and t[k]() flagged.",
  "edges": [
    {
      "source": { "name": "run_literal_dispatch", "file": "dispatch.lua" },
      "target": { "name": "greet", "file": "dispatch.lua" },
      "kind": "calls",
      "mode": "dynamic",
      "notes": "t['greet']() — string-literal bracket index, resolves to local greet()"
    }
  ]
}
