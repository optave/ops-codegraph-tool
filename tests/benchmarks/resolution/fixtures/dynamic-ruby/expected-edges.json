{
  "$schema": "../../expected-edges.schema.json",
  "language": "ruby",
  "description": "Phase 4 Ruby fixture: send(:method) resolved as reflection; send(variable) flagged.",
  "edges": [
    {
      "source": { "name": "run_send_symbol", "file": "dispatch.rb" },
      "target": { "name": "greet", "file": "dispatch.rb" },
      "kind": "calls",
      "mode": "dynamic",
      "notes": "obj.send(:greet) — reflection kind, resolves to top-level greet()"
    },
    {
      "source": { "name": "run_public_send", "file": "dispatch.rb" },
      "target": { "name": "farewell", "file": "dispatch.rb" },
      "kind": "calls",
      "mode": "dynamic",
      "notes": "obj.public_send(:farewell) — reflection kind, resolves to top-level farewell()"
    }
  ]
}
